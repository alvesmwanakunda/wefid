import { DataAsAgoPipePipe } from './data-as-ago-pipe.pipe';

describe('DataAsAgoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataAsAgoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
