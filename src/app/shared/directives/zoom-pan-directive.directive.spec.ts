import { ZoomPanDirectiveDirective } from './zoom-pan-directive.directive';

describe('ZoomPanDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomPanDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
